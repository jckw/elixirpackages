defmodule ElixirPackagesWeb.PowTest do
  use ElixirPackagesWeb.ConnCase

  test "failed github login redirects to homepage", %{conn: conn} do
  end

  test "successful github login redirects to homepage" do
  end

  test "successful github registration redirects to homepage" do
  end
end
